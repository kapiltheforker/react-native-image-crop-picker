<resources>
    <style name="CustomImagePickerTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Ensure content doesn't go under the status bar -->
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:statusBarColor">@android:color/black</item> <!-- or match your app -->
    </style>
</resources>